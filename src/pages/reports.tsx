// ---------------------------------------------------------------------------
// Legacy page registry — no longer used by the accordion-based report flow.
// Kept as an empty placeholder to avoid breaking any stale imports.
// ---------------------------------------------------------------------------

import type { PageDefinition } from "../context/ReportContext.tsx";

const REPORTS: PageDefinition[] = [];

export default REPORTS;
